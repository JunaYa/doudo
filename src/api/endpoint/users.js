const BASE = 'http://localhost:3000/api';

const LOGIN = `${BASE}/users/login`;
const REGISTER = `${BASE}/users/register`;
const UPDATE = `${BASE}/users/update`;

export default {
  BASE,
  LOGIN,
  REGISTER,
  UPDATE
};

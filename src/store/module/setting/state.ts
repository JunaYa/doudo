import { Config, Structure, Theme } from './types';

export const initStructure: Structure = {
  separator: 200
};

export const initTheme: Theme = {
  backgroundColor: '#FFFFFF'
};

export const state: Config = {
  structure: initStructure,

  theme: initTheme
};
